# Local development only
# In CI/CD, this is automatically fetched from CloudFormation outputs
# Replace with your API Gateway URL after first backend deploy
NEXT_PUBLIC_API_URL=https://REPLACE_WITH_YOUR_API_GATEWAY_URL
